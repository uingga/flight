# 인계 메모 — 2026-08-21

하루 동안 크롤 안정성, 보안, 어드민을 손봤다. 다음 사람이 이어받을 때 필요한 것만 적는다.

---

## 지금 당장 해야 할 것

### 지메일 앱 비밀번호 발급 → Vercel — ✅ 완료 확인 (14:00)

문의 폼과 항공권 제보 메일의 공용 인증 설정이 복구됐다.

- Google 계정에 `tikitikit` 앱 비밀번호가 13:56 생성됐고 14:00 최종 사용됨
- Vercel에 `GMAIL_APP_PASS`(Sensitive, Production/Preview)와 `GMAIL_USER` 등록 확인
- 환경 변수 등록 뒤 재배포와 최신 `main` 배포 모두 `Ready`
- Vercel 운영 로그에서 `POST /api/contact`가 14:00:02 HTTP 200으로 완료된 것 확인
- `/api/flight-report`도 같은 환경 변수와 전송 설정을 사용함

원인은 저장소가 공개인 채로 `.env.production`에 앱 비밀번호가 6개월간 올라가 있었고,
그 값이 무효화된 것이었다. 새 비밀번호 값은 저장소나 문서에 기록하지 않았다.

---

## 보류된 결정

사용자가 판단을 미룬 항목이다. 임의로 진행하지 말 것.

### 최소 탑승 인원 표시

노랑풍선 348건 중 **108건(31%)이 2인 이상**이어야 예약된다. 화면에는 1인 기준가만 보인다. 땡처리도 같은 문제이며 특가 절반가량이 해당한다.

값은 이미 수집된다 (`Flight.minPax`, 노랑풍선 상세 응답에서). 표시 방법만 정하면 된다.

### 우회·은폐 조치 정리

코드에 남아 있는 것들이다. 성격이 갈린다.

| 조치 | 위치 | 없애면 |
|---|---|---|
| 거짓 구글 리퍼러 | `ybtour.ts:114`, `hanatour.ts:188`, `onlinetour.ts:33` | 안 깨질 가능성 높음 |
| `navigator.webdriver` 위조 | 같은 세 파일 | 안 깨질 가능성 높음 |
| 네이버 stealth 플러그인 + 화면 밖 창 | `crawl-naver.ts:10,183-188` | **확실히 막힘** |
| 모두투어 내부 API + ApiKey 추출 | `modetour.ts:54-106,190-203` | **확실히 막힘** |
| 땡처리 내부 API | `ttang.ts:98-119` | **확실히 막힘** |

앞의 둘은 필요하다는 근거가 코드 어디에도 없다. 관성으로 남은 것으로 보인다.

### 마이리얼트립 robots.txt

`api3.myrealtrip.com`과 `flights.myrealtrip.com` 모두 `User-agent: * / Disallow: /`다. 하루 약 208건을 보낸다. 파트너 관계가 있으므로 담당자에게 조회 허용 범위 확인 권장.

---

## 미뤄둔 작업

### 콘텐츠 보안 정책 강제 전환

`next.config.js`에 `Content-Security-Policy-Report-Only`로 넣어 뒀다. 며칠 돌려보고 브라우저 콘솔의 위반 내역을 확인한 뒤, 정상 기능이 걸리지 않으면 `Content-Security-Policy`로 바꾼다. 광고·통계 스크립트가 걸릴 가능성이 있어 바로 강제하지 않았다.

### Next.js 14 → 16

`npm audit`의 남은 high 등급 대부분이 여기에 딸려 있다. 주요 버전 두 단계라 문법 변경이 있다. 되돌릴 지점(`npm run deploy:mark`)을 만들고 화면을 하나씩 확인하는 순서로. 반나절 규모.

puppeteer도 주요 버전 변경이 필요하다 (`crawl-naver.ts`가 쓴다).

### 저장소 용량 — ✅ 중복 필드 제거 완료

`git count-objects` 기준 pack이 498.57MiB다. 통합 캐시의 중복 필드는 제거했다.

- `all-flights-cache.json`과 `price-history.json`의 히스토리가 완전히 같은지 JSON 비교로 확인
- 크롤러는 앞으로 히스토리를 `price-history.json`에만 기록
- 현재 `all-flights-cache.json` 2,102,852 → 754,253바이트 (1,348,599바이트, 64.1% 절감)
- 저장소가 `C:\Users\ynal\Dropbox\...` 안에 있어 Dropbox가 `.git`을 동기화한다. 크롤 중 충돌 위험이 있다.

### 배포 안전 지점 명령 — ✅ 미커밋 작업과 공존하도록 수정

`docs/DEPLOY.md`는 코드 수정 뒤 커밋 전에 `npm run deploy:mark`를 실행하라고 하지만,
기존 스크립트는 작업 트리가 깨끗하지 않으면 실행을 거부했다. 진행 중인 Supabase 작업을
스태시하거나 건드리지 않고도 현재 배포 `HEAD`를 안전 지점으로 태그하도록 수정했다.
미커밋 변경은 안전 태그에 포함되지 않는다고 명확히 경고한다.

### GitHub 지원팀 요청 (선택)

이력을 정리했지만 옛 객체가 커밋 SHA로 접근된다. https://support.github.com/contact 에 정리 요청 가능. 다만 자격 증명이 모두 무효화되어 실익은 낮다.

---

## 오늘 바꾼 것

### 크롤 안정성

- **무결성 가드가 발동 불가 상태였다.** 이전 캐시(필터 후)와 이번 수집(필터 전)을 맞비교해서, 땡처리 기준 발동선이 원본 137건이었다(정상 2,078건). 원본끼리 비교하도록 고쳤고 캐시에 `scrapedCounts`를 저장한다.
- **노랑풍선 아시아 탭이 안 열려도 성공 처리**되고 있었다. 도시 목록이 비어 있지 않다는 이유였는데, 탭 클릭이 실패하면 직전 지역 목록이 그대로 남는다. 감지된 도시가 그 지역 것인지 확인하도록 고쳤다.
- **온라인투어·하나투어의 완결성 판정이 항상 "정상"**이었다. 온라인투어는 자기 탭 이름(`아시아`)을 우리 지역 이름(`동남아`)에 대조했고, 하나투어는 탭 이름(`인천/김포`)을 도시 표기(`서울(ICN)`)에 대조했다. 둘 다 공항 코드 기준으로 바꿨다.
- **온라인투어 연도가 `2026`으로 하드코딩**되어 있었다. 12월부터 1월 출발편이 작년으로 기록되어 만료 필터에 전부 걸릴 예정이었다. 크롤 시점 기준으로 추론한다.
- 시각 보강의 실패 경로가 `continue`로 대기와 진행 로그를 건너뛰었다. 매 요청 간격, 지수 백오프, 8회 연속 실패 시 중단을 넣었다.

### 노랑풍선 시각 자체 수집

시각을 땡처리 실시간 검색에서 빌려오고 있었다. 땡처리 요청의 64%가 여기서 나왔고, 빌려온 값이 틀린 사례도 확인됐다(진에어 귀국편 11:00 대 실제 10:55).

`findDscInvSkdDetail.lts`(목록 다음 단계 POST)가 편명·양쪽 시각·수하물·최소 탑승 인원을 준다. 응답 42ms.

- 실측: 262건 조회, 실패 0, 형식 불일치 5건(버림)
- 시각 보유율 35% → 99%
- `ybtour.ts`에 땡처리 참조 0개
- 응답이 JS 문자열 조립이라 마크업 변경에 취약하다. 요청 출발일 일치, 왕복 도시 일치를 확인하고 어긋나면 버린다.
- 복사 조건에 "편명 있음"을 넣어 땡처리 시대의 옛 값이 씻겨 나가게 했다

### 보안

- **어드민 API 4개가 공개 상태였다.** `process.env.ADMIN_KEY || 'tikit2026'`이고 Vercel에 미설정이라, 공개 저장소를 읽은 누구나 접근 가능했다. 폴백 제거(미설정이면 전부 거부) + Vercel에 `ADMIN_KEY` 설정 완료.
- **`/api/redirect`가 오픈 리다이렉트 겸 SSRF였다.** 여행사 도메인 허용 목록 추가.
- `.gitignore`가 `.env`와 `.env*.local`만 막아 `.env.production`이 빠져나갔다. `.env*`로 넓히고 `git filter-repo`로 이력에서 제거 후 강제 푸시.
- 보안 헤더 5종 추가 (CSP는 보고 전용)
- 의존성 취약점 16건 → 8건, 치명 0

### 알림

- **하나투어·노랑풍선 항공권 번호가 화면 순서 기반**이라 매 크롤마다 바뀌었다. 알림이 같은 표를 "처음 보는 표"로 판단해 가격이 그대로여도 반복 발송했고, `/share/{id}`도 다른 표를 가리켰다. 내용 기반으로 바꿨다.
- 노선형 알림에 신선도 검사가 없어 며칠 전 사라진 표로도 발송됐다. 조건형과 같은 72시간 기준 적용.
- VAPID 연락처가 만든 적 없는 주소(`tikitikit.kr@gmail.com`)였다. 사이트 주소로 교체.

### 크롤 시각

**저녁 9시 제거 (하루 7회 → 6회).** 최근 4주 34회차 중 13회(38%)가 새 표를 하나도 못 찾았고 중앙값이 1건이었다. 다른 시각은 16~22건. 여행사 사이트 이용자가 가장 많은 시간대이기도 하다.

마이리얼트립을 10시 → 9시로 옮겼다. 항공권 크롤과 같은 시각이라 같은 캐시 파일을 놓고 푸시가 서로 거부됐다.

실패 알림이 지금까지 한 번도 뜬 적이 없었다. `permissions`에 `issues: write`가 없어 403으로 죽었다. 권한 추가 + 커밋 실패도 잡도록 조건 확장.

### 어드민

4탭 구조로 개편(운영 상태 / 항공권 데이터 / 방문자 / 알림 사용자). 무결성 경보는 탭 바깥 최상단 고정.

**여행사별 수집 상태 카드**를 새로 넣었다. 현재 노출 건수, 마지막 갱신, 최근 16회 수집량 막대. 최근 중앙값 대비 급감을 `수집량 이상`으로 표시한다 — 가드는 직전 한 번과만 비교해서 반쪽 결과가 자리를 잡으면 조용해지는데, 이 판정이 그걸 잡는다.

크롤 히스토리에 `노출 건수 / 수집 건수 / 새로 들어온 표` 토글을 넣었다.

### 기타

- 필터 문구: `출발지 · 지역 선택` → `출발지 · 도착 지역 선택`, 적용 시 `인천/김포 출발 · 일본 도착`
- "지난 방문 뒤 새로 들어온 항공권" 기능 제거 (사용자 요청)
- 개인정보처리방침을 실제 수집·위탁 내용에 맞게 수정
- 트립닷컴 제휴 링크 고지 (푸터 + 링크 툴팁)

---

## 커밋되지 않은 작업

Supabase 장기 가격 기록 기능이 진행 중이다. 오늘 작업과 무관하게 이어서 하면 된다.

```
M  AGENTS.md
M  docs/marketing-strategy.md
?? docs/community-marketing-experiments.md
?? docs/long-term-price-history.md
?? scripts/archive-flight-prices.ts
?? supabase/
```

---

## 확인이 필요한 것

### 15시 첫 운영 회차 검증 완료 (2026-08-21)

- GitHub Actions `Daily Flight Crawl #1367`은 **성공**했다. 전체 작업은 16분 18초, `Run crawler`는 10분 22초 걸렸다(무작위 지연 4분 46초 별도).
- 노랑풍선 Phase 1은 `page.evaluate: ReferenceError: __name is not defined`가 반복되어 **0건**으로 끝났다. 이전 캐시 225건을 보존했고 4회 연속 실패로 기록됐다.
- Phase 1에서 조회 대상이 만들어지지 않아 노랑풍선 자체 스케줄 POST 조회는 **미실행**됐다. 따라서 성공/실패/형식 불일치 수 집계도 생성되지 않았다(0/0/0으로 성공한 것이 아님). 자동 환경에서의 자체 시각 수집 성공 여부는 아직 확인되지 않았다.
- `scrapedCounts`는 생성됐으며 이번에 실제 원본 수집에 성공한 `modetour: 990`, `ttang: 1886`이 기록됐다. 일일 크롤 대상이 아닌 마이리얼트립과 0건 실패로 보존한 여행사는 포함되지 않았다.
- `integrityAlerts`는 3건이다: 노랑풍선 0건(4회 연속), 하나투어 0건, 온라인투어 0건. 세 여행사는 이전 데이터를 보존해 사이트 노출 건수가 각각 225/39/6건으로 유지됐다.
- 어드민 중앙값 판정의 `수집량 이상` 대상은 없었다. 실패·보존 회차는 이 판정에서 제외되므로 세 여행사는 각각 `수집 실패`로 표시되고, 정상 수집한 모두투어·땡처리닷컴·마이리얼트립은 최근 중앙값 대비 급감 조건에 해당하지 않았다.
- 크롤 결과는 데이터 커밋 `df7ab9a`로 푸시됐다. `git pull --ff-only` 후 `data/all-flights-cache.json`은 694건이며 `priceHistory` 필드는 없고, `scrapedCounts`와 `integrityAlerts`는 정상 포함돼 있다.

## 백업

이력 정리 전 상태의 번들이 임시 폴더에 있다. 임시 폴더는 언젠가 지워지므로 필요하면 옮길 것.

```
%LOCALAPPDATA%\Temp\claude\...\scratchpad\flight-backup-20260821-1308.bundle
```
