# 수집 신뢰성 1차 보완 — 2026-09-26

## 상태

사용자 승인 후 **웹 코드 배포와 A/B/C 운영 실행본 설치를 완료했다.** 추가
수동 수집은 하지 않았다. 다음 정규 회차에서 실제 수집·게시 결과를 확인해야
하며, 오프라인 검증만으로 모든 여행사의 무인 운영 성공을 보장하지 않는다.
원본 작업 폴더의 기존 미커밋 변경은 그대로 보존했다. 별도 `crawl-reliability`
작업 폴더에서 준비하고 최신 main `10464458d4167a8d38bfed122685203802f045eb`의
자식 `b1c9e2b731d6429dc48827f995f4a66671ea32af`를 정식 배포 경로로 발행했다.
해당 커밋의 공통 writer 확인과 Vercel 배포 성공을 확인했다.

## 수정한 범위

1. **땡처리 20:30 저장 경로:** 회차·여행사별로 실제 체크포인트가 허용하는
   `.local-crawler/staging` 하위의 독립 폴더를 만든다. 경로 우회, 기존 폴더
   재사용 및 입력 덮어쓰기는 거절한다. 새 파일이 빠진 설치 패키지도 거절한다.
2. **저녁 성공분 전달:** 뒤 여행사가 실패해도 앞의 검증된 결과를 반환한다.
   실패한 하위 프로세스가 캐시를 지워도 앞의 결과를 오염시키지 못한다.
   A는 기존 검증·부분 병합·공통 writer 경로를 사용한다. 접근 제한이나 종료
   불명확 이후에 다른 여행사 요청을 계속 보내지는 않는다.
3. **PC/MRT 게시 대기:** 로컬 게시 서비스가 변경 전에 명시적으로 반환한
   `PUBLICATION_BUSY`만 최대 10분 기다린다. 요청 ID/본문은 동일하다.
   네트워크 불확실, 일반 409, 인증 오류, 429, 완료된 거절은 새 실행으로
   재시도하지 않는다. 기존 원격 영수증 확인과 불명확 상태 보존을 유지한다.
4. **네이버 중단 후 안전 마감:** 현재 설치본을 기준으로 별도 패치를 만들었다.
   A·C 브라우저 종료 및 결과 저장 증거가 있고 미결 요청·명시 차단이 없는
   일시 오류일 때만 검증된 성공분을 게시한다. 운영 재확인 후 중단 회차를
   마감하고 다음 정규 회차로 연결한다. 사용량·기존 키·실패 기록은 보존하며,
   추가 조회와 오늘의 표 선정은 하지 않는다.
   중단 기록에는 실제 확인한 수만 남기고 미확인 대기 건수를 0으로 만들지
   않는다. 기존 실행 기록이 있으면 중복 기록하지 않으며, 어드민의 필수
   여행사/실행기 필드도 유지한다.

네이버 운영본은 저장소보다 앞선 병렬 실행·복구 기능을 포함한다. 따라서
저장소의 오래된 실행본으로 통째로 덮지 않고
`scripts/runtime-patches/naver-safe-partial-20260926/`에 정확한 기준 해시와
별도 패치를 보존했다. A/C 설치본에는 정확한 파일 해시를 대조한 후 해당
변경만 적용했다. 이후 발견한 브라우저 함수의 변수명 충돌도 실제 수집 전에
수정했고, 최종 패치와 A/C 설치본의 일치를 다시 확인했다.

## 검증

- 여행사 저장·부분 결과·게시 대기·영수증·전송·중단 기록 회귀: **63/63 통과**.
- 네이버 실제 실행기/조정기/게시기와 안전 거절 경로: A 설치본 **41/41 통과**.
  C 설치본도 14개 기존 검사와 27개 새 통합 검사를 통과했다. C의 Node 22
  테스트 자식 프로세스에서 직렬화 오류가 발생해, 같은 27개 검사를 조건 변경
  없이 단일 프로세스로 실행했으며 모두 통과했다.
- B/C 설치된 저녁 워커: 각각 **5/5 통과**. A source-fallback 및 MRT의 실제
  게시 클라이언트: 각각 **8/8 통과**. 임시 파일과 모의 응답만 사용했다.
- 네이버 실행기·게시기·런타임 설정의 엄격한 TypeScript 검사: 통과.
- 웹앱 `npm run build`: 통과. 기존 Browserslist 및 react-datepicker 경고는 남아 있다.
- 네이버 최종 패치: 설치 전 백업 복사본에서 적용 검사 통과, LF 정규화 후
  12개 결과 파일 해시 일치. 모든 변경 MJS 파일의 구문 검사도 통과했다.
- 별도 기존 HTTP broker 테스트 1개는 변경 전 설치본에서도 실패한다. 가짜
  Git ref가 현재 40자리 SHA 계약을 따르지 않는 오래된 테스트 도구 문제이며,
  위 통과 수에 포함하거나 운영 장애로 단정하지 않았다.

## 운영 적용 확인 — 2026-09-26 02:52 KST

- B SSH 연결이 정상화되어 A/B/C 모두 현재 상태를 직접 확인했다. 수집·게시가
  없는 상태에서 설치했고, 유휴 A 조정기만 재시작했다. 예약 시각은 변경하지 않았다.
- A/C 네이버 전체 실행본 서명:
  `373597065c963ca206a76ebd238cfae0e865ec03ea14809795dab620f69fe59e`.
- B/C 저녁 패키지 및 A 배포기 고정 버전:
  `3b9ba99a6d981833206130c0acdc66d3d2e7bde5b109a5ade176ded8474e492c`.
- MRT 게시 클라이언트 패키지 및 A 실행기 고정 버전:
  `daf23d0f51d11ef1a4f81db8242410a0e16dcd84f4501335b850ed7d6fc737be`.
- 조정기 인증 상태 조회 정상. 전체 날짜별 조회 원장이 설치 전과 동일하고,
  9/26 사용량 A 0/C 0, 미결 요청 0, 게시 잠금 없음, 당일 차단 없음이다.
  B/C의 기존 처리 키·보호 파일도 해시가 유지됐다. 인증과 ACL은 바꾸지 않았다.
- 변경 전 파일은 각 PC의 `deployment-backups/crawl-reliability-20260926` 및
  A/C의 `crawl-reliability-profile-fix-20260926`에 되돌릴 수 있도록 보관했다.
- C의 기존 `ttang-unknown.json`은 지우지 않았다. 이 개별 사건의 재개 판단은
  별도로 남아 있으며, 코드 설치 완료를 기존 보호 해제로 오인하지 않는다.

## 다음 과제

온라인투어의 빈 목록/월 판독, 트립닷컴 누락 회차·재시작 경로·일부 카드
판독·게시 표시 불일치는 아직 별도 보완 대상이다. 이번 수정만으로 모든
여행사의 무인 운영이 영구적으로 보장된다고 보고하지 않는다.
