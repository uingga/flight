# TE31 부산 출발 여러 노선 묶음

- 코드: `pus-260908`
- 배포 후 사용할 URL: https://www.tikitikit.kr/c/te31-pus-260908
- 랜딩: `/share-group/pus-260908`
- UTM: `utm_source=te31`, `utm_medium=community`, `utm_campaign=tikitikit_te31_pus-260908`, `utm_content=share_group_pus-260908`
- OG: `부산 출발 특가 5개`. 소개한 5개 노선의 모음이며 현재 재고 수량을 보장하는 문구가 아니다.

2026-09-08 운영 `/api/flights?limit=1000`에서 아래 ID와 가격을 확인했다. 등록값은 ID 선택용이며 화면의 가격·일정은 최신 활성 캐시를 사용한다. 운영 캐시 수정이나 추가 크롤링은 수행하지 않았다.

| 노선 | API 가격 | ID |
|---|---:|---|
| 부산 → 장가계 | 199,000원 | `ttang-7C8253PUSDYG-G11-2026-09-15`, `ttang-7C8253PUSDYG-G10-2026-09-19`, `ttang-7C8253PUSDYG-G10-2026-09-26` |
| 부산 → 오사카 | 199,900원 | `modetour-JPN-20178714` |
| 부산 → 타이중 | 258,000원 | `modetour-CHI-20107438`, `modetour-CHI-20107441`, `modetour-CHI-20107439` |
| 부산 → 시즈오카 | 262,000원 | `modetour-JPN-19972825` |
| 부산 → 광저우 | 280,000원 | `modetour-CHI-20081236`, `modetour-CHI-20081233`, `modetour-CHI-20081235`, `modetour-CHI-20081239` |

장가계는 ID가 지정되지 않아 해당 가격으로 확인된 3개 일정을 등록했다. 기존 사이트 카드에는 199,000원이 표시되며 내부 비교 함수는 땡처리 발권수수료 20,000원을 별도로 더한다. 이 작업에서는 기존 가격 표시 방식을 변경하지 않는다.

## 구현과 검증

`ShareGroup`에 선택적 묶음 제목과 노선별 ID 목록을 추가했다. 단일 노선 `pqc1438` 동작은 유지한다. ID가 사라지면 해당 카드만 빠지며 모두 없어져도 등록 페이지와 판매 종료 안내가 유지된다. 수동으로 가격·일정을 덧붙이거나 다른 항공권으로 대체하지 않는다.

기존 상세·예약 핸들러를 재사용한다. 상세 열기·뒤로 가기에서도 URL의 네 가지 UTM이 유지된다. GA4는 기존 `sessionCampaignName + sessionSource`로 유입·`detail_open`·`booking_click`을 연결하며 어드민 제목은 `부산 출발 특가 5개 · TE31`로 표시한다. 실제 어드민 집계 코드를 합성 GA4 응답으로 실행해 방문 3명·상세 2명·예약 1명/3회가 분리되는 것을 확인했다. 실제 운영 GA4 수신은 배포와 실제 방문 후 확인해야 하며 테스트 이벤트를 운영으로 전송하지 않았다.

- `npx tsx scripts/test-share-groups.ts`: 12개 ID, 5개 노선, 부분/전체 소멸, 기존 링크, UTM 리디렉션 검증.
- `node scripts/test-share-group-ui.mjs`: 로컬 31860에서 12개 카드·노선 제목·각 노선 상세와 예약 링크·UTM·모바일 폭·OG 응답 검증. 외부 네트워크 요청 차단.
- `npm run build`: 프로덕션 빌드·타입 검사.

배포와 main 병합/푸시는 별도 명시 승인 전까지 금지한다. 다른 어드민 시간대·Threads 작업은 다른 작업공간에 보존되어 있다.
