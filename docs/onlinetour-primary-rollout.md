# 온라인투어 PC 주 수집 / GitHub 대체 수집

## 현재 상태

### 예약 실행 탭 수명 수정 — 2026-09-07

- 17:31 회차는 17:57에 `require_exactly_one_existing_list_tab`으로 상품 조회 전에 실패했다.
  기존 목록 탭이 정확히 1개여야 하는 진단용 전제가 운영 실행에도 남아 있던 문제다.
- 정규 실행과 명시적 수동 1회 실행은 전용 Chrome에 작업 소유 빈 탭을 만든 뒤 요청 가드를
  설치하고 공개 AS 목록으로 진입한다. 첫 응답도 40회 상한에 포함하고 다음 단계에 재사용한다.
  모든 지역·도시·월 조회는 이 target ID만 사용하며 기존 사용자 탭 수에 의존하지 않는다.
  완료/실패 시 작업 소유 탭만 닫고 Chrome과 다른 탭은 유지한다.
- `ONLINETOUR_MANUAL_ONCE=1`은 운영자 요청 시 `--sources=onlinetour`에만 허용한다.
  B의 KST 날짜별 독점 표식으로 1회만 실행하며 예약 표식·공유 잠금·차단 회로는 지우지 않는다.
  예약 실행기는 이 환경 변수를 설정하지 않는다.
- 캐시의 직전 정상 원본 수(급감 비교 기준)와 이번 실행 수집량을 분리했다.
  조회 전 실패는 실행 기록에 0건으로 남기고 기존 95건을 이번 성공 수집량처럼 표시하지 않는다.
  온라인투어 실패 상세 원인도 별도로 기록한다.

### 수정 후 사용자 요청 단일 수집 결과

- run `59c8504b-c69f-4ebe-8f02-acef9db03d7f`: 2026-09-07 19:08 KST 완료.
  상품 35/40회, 지역 전환 6회, 7지역 및 해당 도시/월 범위 완료, 정리 성공.
- 원본 195건, 오늘부터 60일 내 출발 175건. 최저가 필터 후 52건이 모두 기존 인터파크
  월평균 기준을 초과하여 최종 0건이다. 가격 기준을 완화하거나 실패로 덮지 않는다.
  이전 온라인투어 1건은 최신 결과에서 제외됐고 다른 여행사 595건은 내용 그대로 보존한다.
- 17:57 실패 기록의 `scraped:95`를 실제 조회 전 중단에 맞게 0으로 정정하고 정정 이유를
  해당 기록에 남겼다. 기존 원본 기준선은 별개이며 실패를 성공으로 바꾸지 않았다.

> 월 범위 검증 정정: 아래 16:35의 ‘7지역 완료’ 판정은 9월의 빈 목록만으로 이후 월까지 완료 처리한 오류가 있었다. 기존 합성 증거는 운영 승인에 사용할 수 없다. 현재 검증기는 모든 빈 지역 월과 모든 도시의 연속된 월별 응답을 요구하며, 40회 상한으로 수정본을 별도 검증한다. 기존 원본 기록은 감사 목적으로 보존한다.

2026-09-07 월별 전체 검증, B 워커 설치, A 연결 설정 및 GitHub 대체 워크플로 배포 후
`src/lib/browser-primary-config.mjs`의 `enabled=true`로 전환한다. 운영 데이터에 검증용 원본을
수동 병합하지 않으며 다음 정규 회차부터 PC 주 수집을 적용한다.

## 승인된 규칙

- PC 주 수집: KST 08:17·11:12·14:23·17:31, 오늘부터 60일 이내 출발.
- 상품 요청은 사용자 승인에 따라 회차당 최대 40회, 지역 전환 최대 6회, 자동 재시도 0회다.
- 빈 지역도 현재 월에서 멈추지 않고 60일 범위의 모든 월을 확인한다. 빈 월의 확인 기록이 빠지거나 도시별 중간 월이 없으면 전체 성공으로 인정하지 않는다.
- 월 버튼이 없을 때는 현재 공개 문서의 `nextMonth` 함수가 알려진 주소 구성과 정확히 일치하는 경우에만 바로 다음 월 문서로 이동한다. 임의 API 호출이나 접근 제한 우회는 하지 않는다. 다음 회차 시작 시 지난 회차의 미래 월 화면은 관찰된 지역 링크로 현재 월에 복귀한다. 월 이동/시작 복귀 문서는 목록 이동으로 따로 기록하고 상품 요청은 동일 40회 상한을 공유한다.
- A의 기존 `TikitikitBlockedSourceCrawl`이 일반 GitHub 회차 반영을 기다린 후 B에 SSH로
  온라인투어만 전달한다. B에 중복 스케줄러를 만들지 않는다.
- PC 성공: GitHub 온라인투어 수집 없음. 일반 GitHub 크롤은 다른 소스만 수집한다.
- PC 실패: 실패한 회차에는 GitHub 대체, 다음 회차는 휴식, 그다음 회차는 다시 대체.
  연속 실패 동안 최초 실패 회차를 기준으로 교대하며, PC가 정상 복구되면 종료한다.
- PC와 GitHub의 24시간 차단 회로는 별도다. 한 환경의 성공으로 다른 환경의 회로를 닫지 않는다.
- GitHub 대체가 실패하거나 휴식 회차이면 이전 정상 데이터를 보존한다.
- PC 수집 종료가 확인되지 않은 SSH 끊김/시간 초과는 즉시 GitHub로 넘기지 않는다.
  `githubFallbackSafe=true`가 있는 확정 실패만 넘긴다. 불명확한 실행 상태는 확인이 필요하다.

## 중복 방지 / 운영 데이터

- B: 호스트 공용 잠금과 삭제하지 않는 회차 실행 표식. 실패해도 동일 회차 재조회 금지.
- A: PC 결과를 main에 반영한 뒤 전용 GitHub 워크플로를 디스패치한다.
  PC 차단으로 요청하지 않는 회차에도 대체 정책을 확인한다.
- GitHub: 일반 크롤과 같은 concurrency 그룹. main 최신 실패 상태·교대 회차·차단 회로를 검사하고
  `onlinePrimary.githubAttemptAt/githubClaim`을 먼저 커밋/push한다. claim push 실패면 수집하지 않는다.
  실행 직전 다시 확인하고 같은 run의 재실행도 거부한다. 뒤늦게 도착한 이전 회차 요청은 건너뛴다.
- GitHub 대체는 `--sources=onlinetour`만 사용하고 인터파크·네이버를 추가 호출하지 않는다.
  기존 GitHub 스크래퍼의 수집 범위를 임의 확대하지 않으며 반환 후보에도 60일 출발 필터를 적용한다.
- 상태: `onlinePrimary`는 PC 성공/실패·회로·GitHub claim, `sourceCircuits.onlinetour`는 GitHub 회로.
  `fullCrawlUpdatedAt`은 부분 수집으로 갱신하지 않는다.
- 일반 GitHub 회차 완료 시 온라인투어는 최신 원격 데이터를 다시 병합한다.
  어드민 기록은 `pc_primary`, `github_fallback`, 기존 `pc_fallback`을 구분한다.
- A가 꺼졌거나 PC 결과를 main에 게시하지 못하면 자동 디스패치도 진행되지 않는다.
  이 경우 실패로 추정해 중복 수집하지 않고 기존 데이터를 보존한다.

## 배포 전 남은 조건

1. 월별 보완 검증 완료: `322c756d-c781-4eb4-8462-599da527850e`, 16:55~17:01 KST,
   상품 34/40회, 지역 전환 6회, 재시도·불완전 페이지·중복·거부 요청 0건. 원본 199건, 60일 내 179건.
   GS/HN/US는 각각 9·10·11월 빈 목록을 확인했다. JA는 김포→하네다 9월 8건·10월 7건·11월 0건이다.
   CH/AS/JA/EU의 7개 도시도 9·10·11월 총 21개 도시/월 범위를 모두 확인했다.
2. B 워커 및 의존 코드 해시 대조. 전용 Chrome만 사용하고 개인 프로필 복사나 로그인 정보 추출 금지.
3. A 전용 자동화 checkout의 `.local-crawler/onlinetour-remote.json` 설치 및 접속 경로 검증.
   protocol `20260907.1`, enabled true, host `tikitikit-pc-b`, workerRoot
   `C:/Users/ynal/AppData/Local/Tikitikit/crawler-validation-20260907`.
4. A의 기존 GitHub 자격 증명에 Actions 디스패치 권한 확인. 자격 증명을 로그/파일에 기록하지 않는다.
   불확실한 디스패치 응답은 같은 회차에 자동 재시도하지 않는다.
5. `npm run test:onlinetour-primary`, 브라우저 회귀, 빌드 및 최신 main 통합.
6. 안전 지점 후 전용 워크플로를 먼저 배포하고 A/B 준비가 확인되면 소유권 플래그를 전환한다.
   새 primary 플래그만 먼저 켜서 일반 GitHub 수집이 사라지는 배포를 하지 않는다.

## DYG 검증 오류 해결 — 2026-09-07

- 실제 공개 목록에서 상태 `01`인 사천항공 표도 예약가능으로 표시하지만 검증기는 `00`만 허용했다.
  현재 목록 문서의 `dCairAllListStr` JSON과 화면 카드 예약 상품번호를 대조해 20개 원본을 복구했다.
  JavaScript 문자열을 실행하지 않고 JSON.parse만 사용했고, 다른 페이지나 로그인 데이터는 읽지 않았다.
- 증거 `18c86649-cbb4-45a4-ba06-60695aa920ec`에서 기존 검증기는 0·1·2·14~19행을
  모두 `unsupported_event_status`로 거부했다. 기존 전체 실패의 9개 위치와 정확히 일치한다.
- 확인된 `00`/`01`만 허용하도록 수정했다. `03`/`05`, 미확인 코드, 비정상 가격·시간·날짜·좌석은
  계속 거부한다. 순회 로그에도 고정 코드 `validationReasons`를 보존해 다음 진단에서 원인을 잃지 않는다.
- 오프라인 재검증에서 기존 11건은 완전히 동일하고 9건이 추가 통과해 20/20이다.
  화면 가격·왕복 네 시각·좌석·출발/귀국 날짜·예약 링크의 상품번호를 대조했다.
  최초 실패 파일을 성공으로 덮어쓰지 않았으며, 복구 증거를 과거 API 응답으로 주장하지 않는다.
- 진단과 검증의 사이트 재요청은 0회, 운영 병합/스케줄 변경/배포도 없다.
  검증: `tsx scripts/verify-onlinetour-status-recovery.ts --snapshot <복구 증거 디렉터리>`.

## 수정 후 전체 검증 — 2026-09-07 16:15 KST

- 사용자 승인 후 단 한 번 실행했다. 기존 실행 표식을 유지하고 정확한 DYG 실패 run에 연결한
  별도 `validation-after-status-fix` 표식을 독점 생성했다. 다른 날이나 다른 실패의 재실행에는 사용할 수 없다.
- run `e81bdc67-f024-4347-b3f7-7568e3d0907a`: 상품 요청 19/20, 지역 이동 4/6,
  재시도 0. CH/AS/JA/EU의 7도시·14개 도시/월 범위를 정상 완료했다.
  원본 199건 중 60일 출발 조건 내 179건, 조건 밖 20건. DYG는 9~11월 90건 전부 검증 통과.
- HN 이동에서 `transportEndCity=''`를 요청 전 검증기가 거부했다(`invalid_api_scope`).
  실패한 HN 상품 요청은 전송하지 않았으며, 차단 신호 없음·cleanupConfirmed=true.
- 읽기 전용 진단에서 HN의 도시 선택 0개, 공개 문서 `dCairAllListStr=[]`, 목록 DOM 비어 있음,
  사이트 요청의 HN/ICN/빈 도시/202609 및 정상 콜백을 확인했다. 진단 추가 사이트 요청 0회.
- 빈 도시가 명시된 사이트 요청만 원문 그대로 인정하되, API의 정상 빈 응답·최종 문서의 명시적 빈 목록·
  숨겨진 것을 포함한 도시 선택 없음·출발지/월/필터 일치를 모두 요구하도록 보완했다.
  빈 화면만으로 성공시키지 않으며, 전체 원본 0건/급감 검사는 유지한다. 이 보완은 아직 실사이트 재검증하지 않았다.
- 전체 검증은 실패 그대로 보존했다. HN/US/GS 미완료이므로 운영 반영·스케줄 전환·커밋/배포하지 않았다.
  남은 상품 예산은 1회뿐이며, 추가 실행이나 상한 확대를 자동으로 하지 않는다.

## 합계 30회 승인 후 남은 지역 검증 — 2026-09-07 16:35 KST

- 사용자 추가 승인으로 앞선 19회를 포함한 합계 30회 상한을 적용했다. CH/AS/JA/EU 재조회 없이
  HN/US/GS만 확인했으며 추가 상품 2회·지역 이동 2회, 총 상품 21회·지역 이동 6회다.
- HN의 빈 공개 문서에는 API 거부 뒤 더보기만 남아 있었다. 도시 항목 0개·카드 0개·명시적 공개 목록
  `[]`를 확인하는 판정을 적용했다. 더보기 버튼 존재만으로 상품이 있다고 추정하지 않는다.
  최초 재개 시도는 사전 검사에서 끝났고 사이트 요청은 0회였다. 그 표식도 보존했다.
- suffix `e4311c33-7077-4f68-b9f7-6eb59cb64a1d`의 세 지역은 모두 `completed=true`,
  `emptyInventoryVerified=true`이며 실제 API/문서 접근 오류, 행/페이지 실패, 미완료 페이지는 없다.
  다만 부분 구간 전체가 0건이라 전체 소스용 `empty_catalogue` 검사가 오작동했다.
- 원본 실패 기록을 수정하지 않고 앞선 정상 구간과 별도 결합 증거
  `6fc1fb98-03fa-4888-ad1e-6edff8a5f1d2`를 생성했다. 7지역 완료·원본 199건·60일 내 179건을
  재검증했고, 차단/지역 누락/예산 초과/정리 실패/불완전 페이지를 넣은 음성 검사 5개도 통과했다.
- 부분 0건 오분류로 생성한 B cooldown은 해당 실행·내용을 대조해 별도 감사 파일로 보존한 뒤
  활성 경로에서만 제거한다. 실제 접근 제한 회로를 닫는 예외가 아니다. 운영 전환 전까지 추가 실수집은 없다.
