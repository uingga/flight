# 인수인계 — 2026-09-04 ~ 09-06 Hermes 봇 운영 기간 요약

> 대상: 이 저장소를 이어받는 Codex / Claude Code / 사람.
> 이 문서는 Discord 채널(`티키티킷_*`)에서만 오간 결정과 진행 상황을 저장소로 옮긴 것이다.
> 아래 내용 중 "티보 보고"로 표시된 것은 크롤러 PC 봇의 자기 보고이며, 원격 저장소에서 확인되지 않았다.
> 사실 확인이 필요하면 `git log`, `.local-crawler/staging/*/summary.json`, 실제 코드를 우선한다.

## 0. 먼저 읽을 것

1. `HERMES_START_HERE.md` — 프로젝트 전체 구조, 안전 경계
2. `docs/hermes-context/data-and-operations.md` — 크롤링·운영 (땡처리 브라우저 크롤러 포함)
3. `docs/hermes-context/current-product.md` — UI, 어드민 수집 그래프 사양
4. 이 문서 — 그 뒤 3일간 무엇이 바뀌었고 무엇이 열려 있는지

## 1. 작업 체계 (2026-09-05 확정, 소유자 결정)

- 봇 3개: 보리스(조율·문서·기획, A PC), 샘(UI·개발, A PC), 티보(크롤러 실행·크롤러 코드, B PC = 크롤러 PC).
- 브랜치: 샘 `sam/*`, 티보 `tibo/*`. 소유자가 "합쳐"라고 하면 담당 봇이 직접 `main` 병합. 충돌은 봇이 풀고 판단이 필요할 때만 질문.
- **배포는 반드시 소유자에게 묻고 한다** (09-06 명시 규칙). 자동 배포를 유발하는 `main` 푸시도 포함. 문서만 바꾼 커밋은 배포 승인 불필요.
- 운영 폴더 `~\Tikitikit\*`(스케줄러 런타임)는 개발 clone과 분리. `.local-crawler/staging/` → 운영 캐시 자동 병합은 계속 금지.
- 큰 UI 변경은 미리보기 URL로 승인받은 뒤 운영 반영.
- 소유자 방향(09-06): Hermes 봇은 조율·감시·콘텐츠에 쓰고, **코드 작업은 Codex/Claude Code를 PC에서 직접** 돌리는 쪽으로 이동. 크롤링 실행은 B PC로 분리 (아래 6절).

## 2. Git 상태 (2026-09-06 19:30 KST 확인)

- `origin/main` `2c57c68f` — 최신 기능 커밋은 `6d49bb97`(어드민 실패 표시 유지), `84957137`(어드민 회차 축 그래프). 그 위는 `chore(data)` 자동 커밋만.
- `origin/codex/ttang-browser-runner` `f33bd746` — main보다 약 25커밋 앞. 내용: 땡처리 로컬 브라우저 크롤러(`b89760ca` Hermes 원격 실행자 포함), DROP 히어로 미리보기(`53a2d807`~`f33bd746`), 도시 이미지 6장(`e6d39dbd`), 인수인계 문서들. **main에 합쳐지지 않았고 운영 배포되지 않음.** 이 브랜치의 `src/app/admin/*`은 main보다 오래됨(어드민 그래프 작업이 main에서 먼저 진행됨) — 병합 시 어드민 파일은 main 쪽을 유지해야 한다.
- `origin/sam/admin-source-slot-axis` — `84957137`로 main에 이미 반영됨. 삭제 가능.
- `origin/docs/bot-roles-20260905` — `46781d68`로 main에 반영됨. 삭제 가능.
- 원격에 `tibo/*` 브랜치 없음. **티보 PC의 크롤러 수정본은 아직 어디에도 푸시되지 않았다** (3절).
- A PC 작업 폴더 `260207_Test`는 `codex/ttang-browser-runner` 체크아웃 상태, 로컬에서 1커밋 앞(미푸시 — 확인 필요).

## 3. 땡처리닷컴 브라우저 크롤러 (티보, B PC) — 티보 보고 기준, 원격 미확인

배경: `docs/ttang-local-browser-crawler.md`, `docs/hermes-remote-crawler.md`. 디버그 Chrome(CDP `127.0.0.1:9222`)에 붙어 목록→상세를 순차 조회, 결과는 `.local-crawler/staging/`에만.

### 3.1 상세 조회 버그 수정 (09-04)
- 원인: `src/lib/ttang-product-schedule.ts`가 `scheduleAct.do`에 `hanaFareId=<id>`만 보냄. 실제 사이트는 `fareRec1=<id>` + 공항·날짜·항공사·인원·좌석등급. 서버 `E001` 응답을 "형식 변경"으로 오분류. 차단 아님.
- 수정·테스트·타입·빌드·독립 검토 통과 (09-04 17:24). `OK+빈 일정`은 "결과 없음"으로 분리, 현재 좌석 0이면 과거 좌석 제거.

### 3.2 부분 성공 보존 (09-05)
- 중간 오류·차단·강제 종료 시에도 성공한 상세 결과는 staging에 보존. 실패·빈 결과·옛 값은 섞지 않음. 부분 성공이어도 회차는 실패로 표시.
- Windows `EPERM`(임시 파일 교체 거부, Dropbox 밖에서도 재현) → 교체만 최대 2.55초 재시도, 임시 파일명 매번 변경. 테스트 34개 통과.

### 3.3 연속 실패 정책 (09-05)
- 401/403/429/CAPTCHA 1건 즉시 중단 / 동일 `E001` 3연속 중단 / 형식 오류 3연속 / 혼합 오류 최대 8.
- 실패 대기 4~6.4 → 8~12.8 → 16~25.6 → 30~48초, 정상 응답 시 초기화. `E001`이 24시간 차단 회로를 여는 문제 차단.
- 요청 간격: 1건씩, 4~8초 무작위, 10건마다 30~60초 휴식. 실행 제한 30→60분, 상세 상한 20개 → 전체 후보.

### 3.4 실사이트 실행 결과
- 09-04 파일럿: 목록 1,282 / 상세 8연속 실패 (버그) / `failed_validation` 격리.
- 09-05 18:44 1차: 목록 1,395 / 후보 151 / 성공 10 / EPERM으로 중단.
- **09-05 19:26 2차: 28.7분, 목록 1,395 / 상세 151 전부 완료 / 성공 97(시간 4개 확보) / 정상 빈 결과 54 / 오류 0.** 97 중 좌석 확인 54. 상태 `ready_for_review`. 운영 반영·커밋 없음.

### 3.5 열린 것
1. **위 수정본 전부를 `tibo/ttang-schedule-fix`(가칭)로 커밋·푸시** — 가장 급함. 티보 PC에만 있다.
2. 2차 결과(97건) 소유자 검토 → 운영 반영 방식 결정. 부분 성공분을 운영 캐시에 넣는 별도 병합 기능은 아직 없음(전체 "최신" 도장 오염 위험).
3. 전용 Chrome 프로필의 Adobe 확장/background target 정리 (09-04 지적, 처리 불명확).

## 4. 온라인투어 새 수집기 (티보, 09-06) — 전부 미커밋

- 09-05 17:52 GitHub 수집 107→37 급감으로 보호장치 작동, PC 대체 105건인데 최종 캐시 1건 — **필터 과도 여부 미확인**.
- 구글 로그인된 일반 Chrome에 원격 디버깅 연결 승인(전용 프로필은 구글 로그인 거부). 실조회는 매번 상한을 승인받아 실행.
- 합의: 상품 수 변동 시 재조회 안 함, 일시 오류는 해당 페이지 1회 재시도, 접근 제한은 재시도 없음.
- 파일: `src/lib/onlinetour-list-traversal.ts`, `.local-crawler/verification/onlinetour*`, `scripts/fixtures/*`. 기존 `crawl-onlinetour.ts`는 운영 캐시 직접 수정·시간 검증 느슨 문제 지적됨(미수정).
- 열린 것: 보조 프레임 요청을 수집 범위에서 분리할지 소유자 결정 대기 → 남은 지역 탐색 → 전체 도시·월 규모 산정 → 전체 staging → 운영 연결.

## 5. UI (샘, A PC)

### 5.1 어드민 "여행사별 수집 상태" 그래프 — 운영 반영 완료
- 사양: `docs/hermes-context/current-product.md` (84957137에 포함). 여행사별 이벤트 `.slice(-16)` → **회차 축 공유**(16회차, 08:17/11:12/14:23/17:31), 진행 중 회차 자리 미리 표시, 예정 없음 칸은 흐린 자리표시, 한 회차 여러 이벤트는 최종 결과 하나로, 수동 캡처도 막대, PC hover/모바일 tap 툴팁. 디자인 변경 아님.
- `84957137` 회차 축 + `6d49bb97` 복구 뒤에도 실패 표시 유지 (소유자 지적 "실패도 표기돼야" 반영). 09-06 소유자 승인 후 배포.
- 파일: `src/lib/admin-source-slots.ts`, `src/app/admin/page.tsx`, `admin.module.css`, `scripts/test-admin-source-slots*.ts`.
- 남은 것: 운영 어드민 로그인 후 실제 화면 최종 확인.

### 5.2 TIKIT DROP 히어로 — 미리보기만, 운영 미적용
- `codex/ttang-browser-runner`에만 존재. `/preview/drop-hero?sample=1`. `src/components/DropHero.tsx`, `DropHero.module.css`, `src/lib/city-image.ts`.
- 소유자: "나중에 적용하자고 하면 그때 적용" (09-04). PC 크롭 위치 65%로 조정(`f33bd746`).
- 도시 이미지 6장 추가(`e6d39dbd`): 광저우·반다르세리베가완·연길·사가·이바라키·요나고. 이미지는 Codex 데스크톱 앱(ChatGPT 구독)으로 생성 — API 키 없음.

## 6. 크롤링을 B PC로 분리하는 방향 (09-06 소유자 의사, 미실행)

- 코드는 나누지 않고 **실행만** B PC로. 같은 저장소를 B PC에 받아 운영용 실행 사본과 개발용 clone을 분리.
- A PC의 Windows 예약 작업 2개(`TikitikitNaverCrawl`, `TikitikitBlockedSourceCrawl`)와 땡처리 브라우저 크롤러를 B PC 예약 작업으로 옮기고 A PC 쪽은 끈다. 같은 시각 두 PC가 같은 크롤을 돌리면 안 됨 (IP 차단 위험이 목적).
- 결과 반영은 GitHub Actions와 같은 방식(`data/` 커밋 → Vercel). 처음엔 staging → 승인 → 반영, 안정되면 자동 커밋.
- B PC 도구: 크롤러 코드 개발은 Codex/Claude Code 직접, 티보는 실행·감시·실패 보고로 축소.

## 7. 그 외 결정·사실

- Antigravity(`agentapi`) 위임: 연결 시험은 성공했으나 실제 코드 작업에 쓰인 근거 없음. 09-06 샘 비교 시험: 작은 검사 작업은 직접이 더 빠름(32초 vs 55초). 큰 독립 작업에만 검토 대상.
- 09-06 소유자: 크롤러 개발을 티보→Codex CLI 위임으로 전환하기로 함 (미실행).
- Hermes 봇 효율 원칙: `docs/hermes-context/hermes-agent-efficiency.md` (main 반영됨). Hermes 봇에만 적용, `AGENTS.md`에는 넣지 않음.
- 인수인계 문서 4종(`brand-and-content.md`, `open-and-superseded.md`, `data-and-operations.md`, 크롤러 문서 2종)은 이 커밋으로 main에 처음 올라감. 기준 시점은 09-04이며 3절·4절 내용은 아직 반영되지 않았다 — 티보 푸시 후 갱신 필요.
