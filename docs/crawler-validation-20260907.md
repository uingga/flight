# B PC 크롤러 검증 진행 — 2026-09-07

## 사용자 승인 경계

최신 지시: 크롤러 완성을 진행하되 **운영은 사용자에게 확인받고 실행**한다.
이번 변경은 구현과 검토용 검증뿐이다. 운영 데이터 병합, 정기 실행 등록·이관·중단,
main 반영, push, 배포는 하지 않았다. 기존 운영 크롤러/예약 작업은 그대로다.

## 구현 및 검증

- A 전용 작업공간: `C:/Users/ynal/tikitikit-wt/codex-crawler-recovery-20260906`
- B 별도 검증 폴더: `C:/Users/ynal/AppData/Local/Tikitikit/crawler-validation-20260907`
- B의 기존 `Tikitikit_crawler_antigravity` 소스와 package-lock 수정은 보존했다.
  새 폴더에는 코드만 전달했고 설치된 node_modules를 연결했다. 운영 data/와 인증 파일은
  복사하지 않았다. 초기 tar의 일부 한글 보관 문서는 Windows 압축 해제에서 오류가 났으므로
  이 폴더를 원본 전체의 백업이나 Git 체크아웃으로 취급하지 않는다. 필수 코드·테스트는 확인했다.

### 땡처리

B의 `ttang-20260905T185656Z` 저장 결과를 현재 `ttang-staging-validation.mjs`로 직접 재검증했다.

- 원본 수집 1,395건, 검토 대상 151건.
- checkpoint: 선택 151 / 성공 97 / 빈 결과 54 / 실패 0 / 미조회 0.
- 상품·노선·네 시간·확인 시각·runId 대조 결과 시간 97건, 좌석 54건으로 기존 집계와 일치.
- 이는 **2026-09-06 KST의 저장 증거를 재검증한 것**이며 오늘 새로 수집한 결과가 아니다.
- B에서 실행기·검토 판정 테스트 24개 통과. A에서 시간 큐·상품 일정 파서와 checkpoint/보강
  테스트 13개도 통과했다. 실제 운영 병합은 없었다.
- 최신 운영 참조에는 오늘 08:44 KST PC 땡처리 대체 수집 성공이 이미 기록돼 있다.
  5시간 간격 및 허용 회차를 무시해 추가 실수집하지 않았다. B 기존 로컬 data/는 9월 4일
  기준으로 오래됐으므로 이를 최신 차단 상태로 쓰면 안 된다.

### 온라인투어

- 지역 실패 진단에 쿼리값·프래그먼트·인증정보 없이 도메인/짧은 정적 경로를 보존한다.
  숫자 등 동적 경로는 가린다. **보조 프레임을 새로 허용하지 않았다.**
- `onlinetour-catalogue.ts`와 `crawl-onlinetour-catalogue.ts`를 추가했다.
  명시된 지역 순서, 지역 진입 시 보이는 도시 목록, 각 도시의 화면상 첫 출발월부터
  보이는 다음 달 버튼만 순회한다. `throughMonth`와 도시별 월 상한을 넘지 않는다.
- 지역 이동과 상품 조회를 합산해 한 계획의 예산을 공유한다. 지역 최대 6회, 상품 최대 100회,
  범위당 최대 20페이지, 도시당 최대 12개월은 **설정 가능한 절대 상한**이지 권장 요청량이나
  이번 실사이트 실행 횟수가 아니다. 재시도/확인 요청도 포함하고 기본 간격은 5초다.
- 지역 이동 때 받은 첫 페이지는 같은 프로세스에서 화면의 범위·페이지 상태를 재확인한 뒤
  이어받아 재조회하지 않는다. 다른 화면으로 이동한 뒤에는 재사용하지 않는다.
- 연결부는 동시에 붙지 않는다. 매 연결 종료 후 접근 제한을 다시 확인하고, 실패하면 뒤의
  지역/도시를 실행하지 않는다. 성공/부분 페이지는 개별 UUID checkpoint에 보존한다.
- 사용자 동의 및 유한 계획 없는 실사이트 CLI 실행은 거부한다. 기존 PC 차단 상태를 읽고,
  검증 실행끼리의 PC 공용 잠금과 접근 제한 후 24시간 휴식을 적용한다. 운영 모드는 없다.
- `plannedCoverageCompleted`는 **계획 내 보이는 후보 순회 완료**일 뿐 전 사이트 모든 상품
  수집의 증명이 아니다. `productionReady=false`, `fullCatalogueComplete=false`를 유지한다.
- A/B에서 신규 전체 순회 검사 12개 통과. 기존 온라인투어 adapter 44개, 지역 검사 41개와
  기존 회귀도 통과했다. 실제 캡처 원본이 없는 선택적 replay 1건은 skip이다.

## 아직 완료하지 못한 실사이트 검증

**후속 갱신:** 아래 연결 실패는 앞선 진단 기록이다. 사용자가 Chrome의 실제 원격 디버깅
허용 창에서 동의한 후, 후속 읽기 전용 진단은 연결·대상 탭 확인·현재 화면 읽기·연결 정리까지
성공했다. 상세 결과는 아래 `동의 후 읽기 전용 연결 성공`을 참고한다. 실제 상품 수집 검증은
여전히 미완료다.

B SSH/코드 실행은 정상이다. 두 Chrome 포트도 listening 상태다. 그러나 온라인투어의
일반 Chrome WebSocket 연결은 받아들여지지 않았다. 연결 진단 결과는 `connection_not_accepted`,
사이트 요청 0 / 화면 조작 0 / attach 0이다. 이것만으로 Chrome 승인창이 실제 떠 있다고
단정할 수 없다. 브라우저 연결 동의·상태 확인이 필요하며 보안 설정은 우회하지 않았다.

따라서 `invalid_paused_request`를 일으킨 보조 프레임의 실제 URL/용도는 여전히 미확정이고,
신규 전체 순회도 실사이트에서는 아직 실행하지 않았다. 코드·모의 검증 통과를 실제 성공으로
보고하지 않는다. 연결이 허용되면 첫 진단은 기존 안전한 탭 상태를 읽고 좁은 지역 전환 1회로
문제 요청을 식별하는 것이다. 과거 실행 계획의 남은 예산을 신규 승인으로 재사용하지 않는다.

### 11:45 KST 이후 연결 원인 추가 진단

- 사용자는 연결 허용 창이 없었으며 대신 `기기로 보내기` 창을 보았다고 알렸다. 해당 창을
  디버깅 동의 창으로 취급하지 않았고 기기 전송 동작은 하지 않았다.
- B에서 읽기 전용 확인 결과 일반 Chrome 152의 기본 프로필 프로세스가 발견 파일의
  루프백 포트를 소유하고 있었다. 다른 전용 Chrome이나 SSH 자체의 연결 실패는 아니었다.
- 진단이 모든 WebSocket 오류를 `connection_not_accepted`로 뭉개던 부분을 보정했다.
  HTTP 거절 상태, 제한된 OS 오류 코드, handshake timeout을 구분하며 URL·토큰·계정은
  출력하지 않는다. 진단만 변경했고 크롤러의 연결 동의/보호 정책은 바꾸지 않았다.
- B에서 기존 endpoint 연결 1회만 재확인한 결과 15,018ms 뒤 handshake timeout,
  HTTP 거절 응답 없음, OS 오류 코드 없음이었다. 사이트 요청/화면 조작/attach는 모두 0.
  이는 **명시적 동의 거절 증거가 아니며** 허용 창이 뜨지 않은 정확한 이유는 아직 미확정이다.
- 연결은 정리했고 반복 요청은 하지 않았다. 다음 확인은 B 일반 Chrome의 공식
  `chrome://inspect/#remote-debugging` 설정 화면이며, 재시작·설정 변경·운영 실행은 하지 않았다.
- 진단 코드 구문 검사 및 `git diff --check` 통과. 이 추가 진단은 실수집 검증이 아니다.

### 동의 후 읽기 전용 연결 성공

- 사용자가 제공한 두 번째 창은 `원격 디버깅을 허용하시겠습니까?`라는 실제 Chrome 동의
  창이었다. 앞선 60초 진단은 이미 종료된 상태였으며 후속 목록 inspect도 일반 오류로
  종료돼, 그 실행만으로 승인 전/후 실패 원인을 확정하지 않았다.
- `inspect-onlinetour-stages.mjs`를 추가해 연결, 필수 탭 메타데이터, 대상 attach, 현재 화면
  읽기, 정리 단계를 구분했다. 오류는 고정 코드만 출력하며 계정·탭 제목·토큰·관계없는 URL은
  노출하지 않는다. `--consent-confirmed` 없이 연결하지 않고 수집 승인 함수도 호출하지 않는다.
- 최초 진단 전달은 Windows 원격 명령 길이 제한, 다음은 SSH 시작 디렉터리의 tsconfig alias
  해석 문제로 Chrome 연결 전에 실패했다. 검증 폴더를 작업 경로로 지정한 실제 실행은 성공했고,
  스크립트도 자기 저장소를 기준으로 경로를 고정해 시작 위치에 의존하지 않도록 했다.
- 실제 연결 요청 시작 후 73,022ms에 WebSocket 연결 성공. 필수 기존 목록 탭 1개와 Google
  홈 탭 1개를 URL 메타데이터로 확인하고, 73,043ms에 adapter 준비 완료.
- 73,051ms에 화면 읽기 성공: 지역 `JA`, `ICN/HND/202609`, 다음 요청 페이지 번호 2,
  다음 페이지 UI 존재, 제한 안내 감지 없음. 이는 현재 화면 판정이며 새 수집 성공이 아니다.
- 73,053ms에 연결 정리 성공. `adapterFailure=null`, actions/productRequests/documentRequests/
  permittedProductRequests/blockedProductRequests 모두 0, 프로세스 exit 0.
- 운영 cache·예약 실행·배포는 변경하지 않았다. 보조 프레임 문제 재현 및 신규 catalogue의
  실제 제한 수집은 남아 있으며 별도 유한 범위·요청 예산 확인 전에 실행하지 않는다.

### 전용 Chrome 전달 및 첫 진입 시험 — 12:34~12:46 KST

- 기존 개인 프로필의 1회 복사본 `~/tmp/chrome-debug`를 재사용하는 전용 Chrome으로 온라인투어
  staging 연결을 전환했다. 개인 Chrome 발견 포트, 매 연결 허용 대화상자 경로, Google 홈 탭
  요구를 제거했다. 프로필을 다시 복사하거나 로그인·쿠키·개인 설정을 조작하지 않았다.
- B 별도 검증 폴더의 기존 코드 13개가 이전 A HEAD와 일치함을 확인한 뒤
  `.local-crawler/rollback/dedicated-chrome-20260907T1234`에 보존했다. 전용 연결 관련 코드·검사·문서
  16개를 전달하고 SHA256 일치를 확인했다. B의 다른 package.json 및 과거 문서는 보존했다.
- 읽기 전용 진단은 약 2.98초에 Chrome transport 연결에 성공했다. 목록 탭 0개로 대상 검사는
  실패했지만 동의 창을 기다리는 연결 오류는 아니었다. attach/상품 조회/사이트 이동은 0회였다.
- 사용자가 승인한 1회 목록 시험을 위해 빈 탭 생성 후 Fetch 가드를 먼저 켜는 첫 진입 경로를
  추가했다. 원본 운영 데이터 전체가 아닌 최신 revision·수집 시각·온라인투어 차단 상태만 B로
  전달했다. origin/main `3e6f3a361fe116ca89839276f23268de9ed836fd`, 11:30 KST 일반 크롤 기준
  `sourceCircuits.onlinetour=null`이었다. B의 PC 검증 cooldown/잠금/중복 프로세스도 없었다.
- 12:46:38 KST 종료된 runId `6054e2ad-40dc-4ac4-be77-6bab6ce09f62`는 실패로 보존했다.
  문서 요청 관측 2 / 허용 1 / 차단 1, 상품 관측·허용 0, 원본·매핑 항공권 0,
  `cleanupConfirmed=true`, `productionReady=false`다.
- 정확한 중단 원인은 메인 프레임이 아닌 GET 문서 `https://gum.criteo.com/syncframe`이었다.
  기존의 모든 보조 문서 fail-closed 규칙에 걸렸으며 쿼리값/계정 정보는 저장하지 않았다.
  이것을 온라인투어의 403·429·CAPTCHA 차단이나 원본 상품 0건 응답으로 해석하지 않는다.
- 이후 해당 광고 보조 문서만 전송 전에 차단하고 성공적인 차단 때문에 전체 수집을 중단하지
  않도록 보정했다. 메인 문서·다른 주소/메서드·리디렉션·응답 단계는 예외 처리하지 않는다.
  광고 차단 실패, 상품 초과 요청, 실제 접근 제한은 계속 실패한다. 회귀 48건 통과.
- 전체 오프라인 검사: adapter 45/45, node-test 155 통과·1 skip·실패 0 (그 안의 지역 검사
  파일이 위 48건을 실행한다). skip은 선택적 실캡처 replay가 없는 경우다. 실사이트 재조회는 없다.
- 결과는 B `.local-crawler/staging/<runId>/`에 남겼다. 당일 첫 진입 표식도 유지한다.
  운영 캐시 병합·예약 실행·커밋/push·배포는 하지 않았다. 다음 단계는 새 승인을 받은
  보정본의 1회 검증이며, 아직 실제 가격·시간·좌석 수집 성공이나 전체 목록 완성을 선언하지 않는다.

### 사용자가 추가 승인한 단일 목록 재검증 — 12:57~13:02 KST

- 사용자는 `응 해봐`로 수정본의 실제 목록 1회 검증을 추가 승인했다. 최신 main의
  온라인투어 PC 차단은 없었고 B의 검증 cooldown·동시 프로세스·잠금도 없었다.
- 첫 진입 표식을 지우거나 기존 실행을 반복하지 않고, 기존 실패 증거가 상품 전송 0회·
  광고 보조 문서 중단·정리 성공인지 확인하는 `--recheck-ad-frame-once`를 추가했다.
  별도 당일 1회 표식이 이미 사용됐으며 첫 진입 표식도 유지한다. 접근 제한 실패는 이 경로의
  재검증 대상이 아니고 운영 차단 검사를 그대로 적용한다.
- runId `93a9e484-fa35-4f10-a54c-9865be8ac407`, 12:57:58 KST 종료:
  `unexpected_api_query`, 문서 관측 2·허용 1, 상품 관측 1·허용 0, 차단 2 중 광고 보조 문서 1,
  원본·매핑 상품 0, `cleanupConfirmed=true`, `productionReady=false`.
  광고 수정은 동작했고 이번 실패는 지역 수집기의 API 요청 검증 단계에서 발생했다.
- 추가 사이트 요청 없이 `inspect-onlinetour-request-shape.mjs --read-only`로 기존 목록의
  resource timing과 공개 함수의 정적 설정을 확인했다. 연결·대상 attach/read/detach뿐이며
  새로고침·탭 이동·목록 함수 실행은 없다. 조회 쿼리의 노선/날짜/페이지 예상값 불일치 0,
  중복 키 0이며 필드 이름에 `apiKey`가 있었다. 키 값·쿼리 원문은 출력하지 않았다.
- 지역 수집기의 허용 키에서 `apiKey`만 빠져 있었다. 사이트 자체가 생성한 원래 요청을
  통과시키도록 단일·비어 있지 않은 512자 이내 ASCII 필드 검증을 추가했고, 키 값을 별도로
  생성·교체·저장하지 않는다. 가짜 모의 요청에도 해당 필드를 넣어 재발을 검사했다.
- 지역 검사 49건 및 재검증 증거 검사 12건 통과. 전체 npm 검사: adapter 45/45,
  node-test 156 통과·1 skip·실패 0. TypeScript 프로젝트 및 실행기 검사 오류 0.
  상품 API 요청은 이 후속 코드 수정 뒤 다시 보내지 않았으므로 가격·시간·좌석 수집은 미확인이다.
- 운영 데이터 병합·정기 실행·main 통합·커밋/push·배포는 없었다. 진단 결과를 정상 크롤이나
  원본 0건 응답으로 기록하지 않는다. 각 실패 결과와 승인별 실행 표식은 B 검증 폴더/상태 폴더에 보존한다.

### 사용자 승인 통합 표본 검증 — 13:48 KST

- 첫 페이지 성공 후 같은 데이터로 최대 3개 화면상 도시·9~10월을 이어가는 단일 실행을 구현했다.
  상품 요청 합계 20회, 지역 문서 1회, 도시별 2개월, 범위별 6페이지, 자동 재시도 0회다.
  첫 단계 오류/늦은 접근 제한/정리 실패 시 다음 단계로 넘어가지 않는다. 검증 전용이며
  `fullCatalogueComplete=false`, `productionReady=false`를 고정한다.
- source-state-only 증거의 최신 main revision은 `3e6f3a361fe116ca89839276f23268de9ed836fd`,
  조회 시각 13:46 KST, 일반 크롤 반영 11:30 KST. 온라인투어 PC 차단·B cooldown·동시 실행·
  검증 잠금이 없었다. A/B 전달 코드와 계획/증거 파일의 SHA256 일치 및 B 모의 통합 16건을 확인했다.
- runId `9b09a427-d1aa-471c-b7f0-49d07a8304e5`, 13:48:23~13:48:31 KST:
  `invalid_api_key_shape`, `firstPageVerified=false`, 문서 허용 1, 상품 전송 0, 상품 0,
  도시/월 순회 0, `cleanupConfirmed=true`. 어드민/운영 캐시에는 넣지 않았다.
- 추가 사이트 요청 없이 기존 resource timing의 키 형식만 확인했다. `apiKey` 출현 1회,
  길이 0, 빈 값, 공백 없음, 예상 쿼리 불일치·중복 없음, 제한 안내 없음이다. 키 원문은 저장하지 않았다.
  앞 수정의 “비어 있지 않아야 함”은 근거 없는 제약이었다. 0~512자 ASCII로 보정하고,
  빈 값 통과 시에도 원 요청을 변경하지 않는 모의 회귀를 추가했다.
- 전체 오프라인 회귀 adapter 45/45, node-test 160 통과·1 skip·실패 0, 지역 검사 50건,
  통합 코드 TypeScript 오류 0. 수정본은 B 검증 폴더에 전달하며 실제 사이트는 재실행하지 않는다.
- 기존 실패/당일 통합 실행 표식은 유지하고 잠금은 정상 해제됐다. 자동 운영, 운영 데이터 병합,
  main 통합, 커밋/push, 배포는 하지 않았다. 실제 가격·시간·좌석·링크 검증 완료로 보고하지 않는다.

### 사용자 추가 승인 통합 검증 — 13:56 KST

- 기존 실패 표식을 보존하고 새 승인 식별자 `20260907-1353-empty-key-fix`로 동일한 유한 계획을
  한 번 실행했다. 최신 main `e7e4184c64e94a4d8205e9461d8b2685701c7491`의 PC 차단 없음,
  B 동시 프로세스·잠금·cooldown 없음, 빈 apiKey 수정 코드 해시 일치를 확인했다.
- runId `545740b7-f7bc-4d2f-9899-99952a8423d0`, 13:56:07~13:56:14 KST.
  상품 전송 1회에서 인천→푸꾸옥 20건을 받았다. 이후 Facebook `/tr/` 보조 문서 POST가
  `invalid_paused_request`로 차단돼 전체 실행 실패, 다음 도시/월 요청 0, 정리 성공이다.
  이 광고 요청의 본문·원문은 출력/저장하지 않았다.
- `verify-onlinetour-saved-evidence.ts`로 보존된 원본/매핑 20건을 오프라인 재대조했다.
  가격 469,000~959,000원, 가격·왕복 네 시각·좌석·예약 링크 구조와 상품번호 모두 일치.
  예약 페이지 자체는 열지 않았고 부분 수신을 전체 수집 성공으로 승격하지 않았다.
- Facebook 추적 폼은 정확히 관측된 URL/POST/하위 프레임에 한해 전송 차단만 수행하고
  수집 오류로 오분류하지 않도록 수정했다. 다른 URL·메인 프레임·리디렉션·응답 오류 보호 유지.
  지역 모의 검사 52건 통과. B 검증 폴더에 수정본을 전달했으나 실사이트는 재실행하지 않았다.
- 실제 데이터 수신은 확인됐지만 연속 도시/월 검증은 미완료. 운영 반영·일정·커밋/push·배포 없음.

### 체크포인트 재개 — 14:15 KST, 실제 표본 완료

- 사용자의 사이드 대화 지침을 반영해 첫 페이지 재조회/새 예산 없이 기존 실행의 남은 예산을
  사용하는 재개 경로를 적용했다. 코드·테스트 전달은 묶어서 수행했다. 재개 관련 60개 모의 검사와
  TypeScript 검사 오류 0을 확인한 뒤 B에서 동일한 재개 검사를 수행했다.
- 기존 실행 `545740b7-f7bc-4d2f-9899-99952a8423d0`의 20건을 원본·화면과 재대조했다.
  페이지 숫자뿐 아니라 같은 문서 로드 시각/loader/탭/범위와 카드 20개의 순서·ID·가격·좌석·네
  날짜/시각을 확인했다. 상품 요청 직전 다시 대조하며 실제 제한은 차단 회로를 유지한다.
- 재개 run `690910ff-9553-4a64-a2f4-158d009dd362`, 14:15:21~14:15:51 KST.
  첫 페이지 재조회 0회, 새 상품 요청 3회, 이전 사용을 합쳐 4/20회, 문서 누적 1회, 재시도 0회.
- 화면에서 관측·검증된 도시 선택지는 PQC 1개였다. 푸꾸옥 9월 21건(2페이지), 10월 21건
  (2페이지), 총 42건을 보존했다. 두 달 모두 terminalVerified=true, raw/unique/실제총수 일치,
  failedRow/Request/Page=0. 원본 대조 시 가격·네 시각·좌석·예약 링크 ID 전부 일치했다.
- 부모의 미보존 첫 페이지 총수는 null로 남기고 `initial_metadata_unavailable` 경고를 기록했다.
  결과는 `review_ready_with_changes`, failure=null, plannedCoverageCompleted=true,
  cleanupConfirmed=true, productionReady=false. 부모 실패 기록은 그대로 보존했다.
- 범위별 체크포인트는 `be210a45-17fe-46a9-9c0a-85530d79b1a9`(9월),
  `76fd52c4-9eaa-4ce1-ac15-c5dd35103622`(10월)다. 운영 데이터·일정·커밋/push·배포는 변경하지 않았다.
- 현재 안전 재개 지원은 첫 페이지 수신 후 보조 문서 오분류 중단이다. 다른 단계/실패의 재개를
  이미 지원한다고 주장하지 않으며, 불충분한 체크포인트를 pageNo만 믿고 건너뛰지 않는다.

### 프로필 확인 및 60일 출발 기준 — 14:38~14:48 KST

- 사용자가 프로필 확인·필요 부분 보완·60일 기준·다른 도시 유한 검증을 승인했다. 운영 반영은 여전히 별도 승인 대상이다.
- B의 9222 Chrome 주 프로세스는 기존 `C:/Users/ynal/tmp/chrome-debug`를 사용했다.
  기존 온라인투어 화면에는 로그인 링크가 있고 로그아웃 링크는 없으며 공개 상품 21건이 표시됐다.
  제한 문구 없음, 프로필/화면 점검의 사이트 요청 0. 쿠키·비밀번호·계정 정보는 읽지 않았고
  로그인도 변경하지 않았다. 과거 복사본의 모든 로그인·쿠키가 유효하다는 증명은 아니다.
- 날짜 범위는 KST 오늘부터 +60일 출발까지 양 끝 포함이다. 이번 기준은 2026-09-07~11-06.
  월별 원본과 검증 파일은 그대로 보존하고 `eligible-flights.json`으로 날짜 범위 내 후보만 별도 저장한다.
  귀국일은 60일 경계 밖이어도 허용한다. 월별 응답 특성상 범위 밖 원본이 들어올 수 있으나 후보에서 제외한다.
- AS 화면의 실제 도시 입력/라벨은 PQC 하나였다. 기존 푸꾸옥 42건을 재조회하지 않도록 PQC를 계획에서 제외하고
  AS→JA 전환 1회·새 지역 1도시·최대 3개월·추가 상품 16회·자동 재시도 0으로 제한했다.
  앞선 4회와 합쳐 상품 요청 20회 이내이며, 일반 브라우저 리소스 통신 전체를 20회로 제한한다는 의미가 아니다.
- 최신 main `19996d3d8c21e23ce673dddee0bf47bac4b17161`, 일반 크롤 14:40:56 KST 반영본의 차단 상태,
  B 잠금·cooldown·동시 실행 없음과 전달 코드/계획 SHA256 일치를 확인했다.
- 실행 `c9d53c91-e85e-43d8-b40c-a791c0b99b36`는 14:45:29~14:45:45 KST.
  지역 문서 1회, 상품 전송 0회, 신규 상품 0, `unexpected_api_query`, 정리 성공으로 종료했다.
  푸꾸옥 재조회 0. 운영에 반영하지 않고 실패 기록/실행 표식을 보존했다.
- 추가 접속 없는 화면 진단에서 일본 기본 노선은 **GMP→HND**였다. 지역 진입 API 검사와
  순회기의 인천 고정값이 원인이므로 관측된 ICN/GMP를 지원하고 최종 화면과 응답 노선을 대조하도록 수정했다.
  임의 출발지는 계속 거부한다. 현재 도시가 아닌 다른 도시의 출발지 전환은 별도 검증 범위다.
- 수정 전 날짜/보존/기존 회귀 113 통과·1 skip, TypeScript 오류 0. 김포 보정 지역 모의 검사 54건 통과.
  보정 후 실사이트 재실행은 하지 않았다. 60일 코드 적용과 다른 도시 실수집 성공을 혼동하지 않는다.
  정기 실행·운영 병합·main 통합·커밋/push·배포 없음.

### 김포→하네다 수정본 실수집 성공 — 14:56~14:57 KST

- 사용자 승인 후 기존 일본 화면에서 첫 페이지를 한 번 검증하고 같은 응답을 월별 순회기에 넘겼다.
  푸꾸옥은 재조회하지 않았다. 변경된 실행기/검사/계획/최신 차단 상태 파일 4개의 B 전송 SHA256을 대조했다.
  실행 직전 잠금·동시 작업·차단 상태를 확인했고 실행기 모의 검사 22건이 통과했다.
- 실행 `57df401e-5a26-4f37-9b70-c9e68dbb5b2f`: 14:56:44~14:57:09 KST,
  `review_ready`, failure=null, cleanupConfirmed=true. 상품 요청 2/16회, 진입 문서 1회,
  월 전환 문서 1회, 재시도 0. 앞선 푸꾸옥 요청까지 합쳐 상품 요청은 6/20회다.
- GMP→HND 9월 8건·10월 7건, 총 15건. 각 월 1페이지이고 마지막 페이지와 총수를 확인했다.
  중복·실패 행/요청/페이지 0. 가격 559,000~609,000원, 출발일 09-08~10-22.
- 60일 출발 기준 09-07~11-06 안에 15건 모두 포함된다. 화면의 전진 가능한 월 선택지로
  확인한 것은 9·10월이며, 11월 직접 조회나 전체 도시 수집 완료를 의미하지 않는다.
  `fullCatalogueComplete=false`, `productionReady=false`를 유지한다.
- 보존된 원본과 15건의 가격·왕복 네 시각·좌석·예약 링크 구조/상품 ID를 오프라인 대조했다.
  예약 상세 페이지는 열지 않았다. 이전 성공·실패 증거를 모두 보존했고 운영 데이터·일정·배포는 변경하지 않았다.

## DYG 오류 수정 검증 — 기존 문서 재사용, 추가 사이트 요청 0

- 앞서 미확정이었던 9행 실패 원인을 확정했다. 공개 문서의 `dCairAllListStr`에서
  화면 예약 버튼의 상품번호로 20건을 찾아 복구했다. 증거 UUID는
  `18c86649-cbb4-45a4-ba06-60695aa920ec`, 복구 시각은 2026-09-07T06:52:15.247Z다.
- 기존 검증기는 0·1·2·14~19행의 상태 `01`을 `unsupported_event_status`로 거부했다.
  화면과 렌더링 코드에서는 해당 표들이 예약가능이므로 `00`과 확인된 `01`을 지원하도록 수정했다.
  `03`/`05` 및 미확인 상태, 비정상 가격·네 시각·날짜·좌석 검사는 그대로다.
- 수정 후 A와 B 모두 재조회 없는 오프라인 검사에서 기존 11건 유지 + 복구 9건 = 20/20 통과.
  화면 가격·네 시각·좌석·날짜·예약 URL 상품번호를 대조했고, 원래 실패 파일은 보존했다.
- B에서 활성 수집기/잠금이 없음을 확인한 뒤 검증기·순회기·증거 재검사 파일만 동기화했다.
  3개 파일의 SHA256이 A/B 일치한다. 주 수집 플래그·운영 데이터·일정·배포는 변경하지 않았다.
- 브라우저 회귀: 어댑터 45/45, 통합 176 통과·1 skip·0 실패(지역 모의 54건 포함).
  주 수집/대체 회차 회귀 56+16건 및 `tsconfig.online-primary.json` 타입 검사 통과.
  전체 지역 실제 수집 완료를 주장하는 검증은 아니며, 장가계 행 검증 오류의 해결 증거다.

## 검증 명령

### 주 수집 전환 전체 목록 검증 — 운영 전환 보류

- 사용자 선택: 온라인투어 B PC 주 수집은 기존 08:17/11:12/14:23/17:31 KST 4회,
  오늘부터 60일 이내 출발. PC 실패 시 GitHub 기존 스크래퍼를 대체 수집으로 사용한다.
- 전체 지역 계획 검증 `8403a4bd-ee75-4270-8808-fdf9677d20e5`는 상품 요청 11/20회,
  지역 이동 2회, 정상 매핑 113건 뒤 중국 DYG 9월 첫 페이지에서 중단했다.
  20개 중 9개 행 검증 실패, `list_traversal_failed`, `cleanupConfirmed=true`.
  사이트 차단으로 확인된 것이 아니며, 정상 113건도 전체 목록으로 운영 반영하지 않았다.
- DYG 자식 체크포인트는 `8199d555-d49f-4862-9c44-b74c25d64230`.
  기존 저장기는 검증 탈락 행 원본과 상세 이유를 보존하지 않았다. 현재 화면에서 사천항공 카드와
  실패 행 위치의 대응은 확인했지만 정확한 필드 원인은 미확정이다. 검사를 느슨하게 하거나
  실패 결과를 성공으로 바꾸지 않았다. 이 전체 검증을 재실행하거나 실행 표식을 삭제하지 않았다.
- 후속으로 PC 주 수집→GitHub 대체 정책, 중복 방지 claim, 전용 워크플로/디스패처,
  환경별 분리 회로, 일반 회차의 최신 PC 상태 보존, 어드민 실행 종류 표시를 코드에 작성했다.
  새 정책은 `src/lib/browser-primary-config.mjs`의 `enabled=false`로 잠겨 있다.
  A 연결 설정 설치, B 최종 코드 동기화, 실제 dispatch 권한 확인, 전체 목록 검증 성공,
  main 통합/안전 지점/배포가 아직 남아 있다. 운영 데이터와 작업 스케줄은 변경하지 않았다.

```text
npm run test:onlinetour-primary
npm run test:onlinetour-browser
npm run test:ttang-time
npm run test:ttang-partial
npm run test:ttang-staging
node scripts/verify-ttang-saved-evidence.mjs --staging <보존된 검토 결과 폴더>
node node_modules/tsx/dist/cli.mjs scripts/crawl-onlinetour-catalogue.ts --help
```

Orca 실행 파일이 없어 Antigravity 분담은 사용하지 못했고 Codex가 직접 구현·검증했다.
Orca 설치/다른 실행 파일 우회는 하지 않았다.
