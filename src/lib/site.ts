/**
 * Public canonical origin.
 *
 * Vercel redirects the apex domain to www, so SEO URLs must always use the
 * final, non-redirecting host regardless of environment configuration.
 */
export const SITE_URL = 'https://www.tikitikit.kr';
