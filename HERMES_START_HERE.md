# Hermes가 티키티킷을 이어받을 때 먼저 읽을 문서

> 기준 시점: 2026-09-04 KST
>
> 이 문서는 영구 규칙이 아니라 현재 상황을 빠르게 이해하기 위한 스냅숏이다.
> 사용자의 최신 지시, 실제 운영 코드, 새로 확인된 데이터가 이 문서보다 우선한다.

## 인수인계의 목적

Hermes는 크롤링 전용 도구가 아니라 티키티킷 프로젝트 전체의 주 작업자가 된다.

- 제품 기획과 UI
- 항공권 추천과 데이터 품질
- 크롤링 운영
- 어드민과 분석
- 브랜딩·콘텐츠·마케팅
- 테스트·Git·배포

다른 PC의 Hermes 봇은 이 전체 역할을 대신하지 않는다. 해당 봇은 메인 Hermes가 요청한
브라우저 수집 명령을 실행하고 staging 결과와 진단을 돌려주는 전용 실행자다.

## 문서 우선순위

문서가 서로 다르면 아래 순서로 판단한다.

1. 현재 대화에서 사용자가 가장 최근에 한 지시
2. 현재 운영 코드와 실제 운영 데이터
3. 이 인수인계 문서의 `현재` 항목
4. AGENTS.md와 기능별 운영 문서
5. `실험`, `보류`, `확인 필요`로 표시된 아이디어
6. 과거 채팅, 과거 전략 문서, 보관 브랜치

문서에 있다는 이유만으로 기능을 만들거나 과거 초안을 되살리지 않는다. 코드와 문서가 다르면
먼저 차이를 사용자에게 설명하고, 사용자가 정한 방향으로 둘을 맞춘다.

## 기존 채팅을 합친 방식

| 기존 채팅 | 영구적으로 남긴 위치 |
|---|---|
| 티키티킷_본채팅 | 네 문서의 공통 제품 방향과 열린 과제 |
| 티키티킷_제품_UI | `current-product.md` |
| 티키티킷_크롤링 | `data-and-operations.md` |
| 티키티킷_브랜딩_마케팅 | `brand-and-content.md` |
| 플레이리스트 | `brand-and-content.md`의 `이 여행의 리듬` 실험 |
| 아이디어 | 관련 주제 문서의 `실험·보류`와 `open-and-superseded.md` |

채팅마다 별도 문서를 만들지 않았다. 한 기능이 여러 채팅에서 논의된 경우 가장 최근 결론과 실제
구현 상태를 한 주제 아래 합쳤다. 채팅 원문은 당분간 복구 자료로 남기되 Hermes의 기본 컨텍스트로
매번 불러오지는 않는다.

## 작업별 최소 읽기 순서

### 제품·UI

1. 이 문서
2. `docs/hermes-context/current-product.md`
3. `docs/ui-design-system.md`
4. 실제 대상 컴포넌트와 모바일·PC 화면

### 크롤링·가격·추천·운영

1. 이 문서
2. `docs/hermes-context/data-and-operations.md`
3. AGENTS.md
4. 변경하려는 소스의 기능별 문서와 실제 코드

### 브랜딩·콘텐츠·마케팅

1. 이 문서
2. `docs/hermes-context/brand-and-content.md`
3. 해당 채널의 현재 문서만 선택해서 읽기

### 밀린 작업·보관 브랜치 정리

1. 이 문서
2. `docs/hermes-context/open-and-superseded.md`
3. 해당 브랜치의 커밋 diff

## 현재 Git 상태

2026-09-04 점검 당시 상태다. 작업을 시작할 때 반드시 다시 확인한다.

- 원격 운영 기준: `origin/main`의 `1371183e`
- 로컬 `main`: `31ab5294`, 원격보다 3커밋 뒤
- 현재 작업 브랜치: `codex/ttang-browser-runner`
- 현재 브랜치 HEAD: `b89760ca`
- `b89760ca`에는 Hermes용 땡처리닷컴 원격 실행자와 문서가 들어 있다.
- 이 브랜치는 원격에 푸시됐지만 `main`에 합쳐지거나 운영 배포되지는 않았다.
- 기존에 보관한 WIP 브랜치는 자동 병합 대상이 아니다.

## 작업을 시작하는 기본 절차

1. `git status --short --branch`와 원격 차이를 확인한다.
2. 요청에 필요한 인수인계 문서 하나만 읽는다.
3. 문서의 설명을 믿기 전에 실제 코드·데이터·운영 상태를 확인한다.
4. 변경을 `운영 수정`, `실험`, `문서화`, `데이터 갱신` 중 하나로 분류한다.
5. 관련 없는 미커밋 파일과 자동 수집 데이터를 건드리지 않는다.
6. 테스트와 빌드를 실행한 뒤 변경 범위를 사용자에게 설명한다.
7. 배포 요청이 있을 때만 AGENTS.md와 `docs/DEPLOY.md`의 절차로 배포한다.

## 원격 크롤러 PC와의 역할 분담

메인 Hermes가 판단하고, 크롤러 PC 봇은 정해진 명령만 실행한다.

```text
메인 Hermes
  → 실행 조건·모드·대상 소스를 결정
  → 크롤러 PC 봇에 요청

크롤러 PC 봇
  → 사용자 로그인 세션의 보이는 Chrome 사용
  → staging-only 실행
  → summary와 진단 반환

메인 Hermes
  → 결과 검토
  → 운영 반영 여부 결정
```

땡처리닷컴 원격 실행의 구체적인 명령과 안전장치는
`docs/hermes-remote-crawler.md`와 `.agents/skills/tikitikit-ttang-crawler/SKILL.md`를 따른다.
원격 봇은 운영 캐시 병합, Git 커밋·푸시, 배포를 하지 않는다.

## 안전 경계

- `.env*`의 값, 쿠키, 토큰, 비밀번호를 문서·대화·로그에 출력하지 않는다.
- `data/all-flights-cache.json`은 운영 데이터이므로 임의로 덮어쓰거나 오래된 브랜치의 파일로 되돌리지 않는다.
- 크롤링 실패와 항공권 판매 종료를 같은 사건으로 취급하지 않는다.
- 원본 수집 수, 필터 통과 수, 현재 노출 수, 고유 상품 수를 섞어 말하지 않는다.
- 차단 징후가 있는 수집을 편의를 위해 우회하거나 자동 반복하지 않는다.
- 미리보기 승인 없이 큰 UI 실험을 운영 메인에 적용하지 않는다.

## 주제별 현재 문서

- [제품·UI 현재 맥락](docs/hermes-context/current-product.md)
- [데이터·크롤링·운영 현재 맥락](docs/hermes-context/data-and-operations.md)
- [브랜드·콘텐츠 현재 맥락](docs/hermes-context/brand-and-content.md)
- [열린 과제·보관 브랜치·폐기된 방향](docs/hermes-context/open-and-superseded.md)
